version: '3.8'

name: omnicure-hisadmin

services:
  # HISAdmin Service
  hisadmin-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_CONFIGURATION: ${BUILD_CONFIGURATION:-Release}
    container_name: omnicure-hisadmin-service
    ports:
      - "${HISADMIN_SERVICE_PORT}:80"
      - "${HISADMIN_SERVICE_HEALTH_PORT}:8081"
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT}
    depends_on:
      - oracle-db
      - redis
      - kafka
    networks:
      - omnicure-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "0.50"

networks:
  omnicure-network:
    external: true
